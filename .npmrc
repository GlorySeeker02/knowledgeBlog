engine-strict=true
legacy-peer-deps=true
# Quartz v5 的 @quartz-community 插件包较新，国内镜像同步滞后，单独走官方源
@quartz-community:registry=https://registry.npmjs.org/
# 网络不稳定时自动重试
fetch-retries=6
fetch-retry-maxtimeout=60000
